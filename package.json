{
  "name": "pilates-pulse",
  "version": "1.0.0",
  "description": "Pilates Pulse Admin Dashboard",
  "scripts": {
    "start": "live-server --port=5500 --host=0.0.0.0 --open=/index.html --wait=100"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
